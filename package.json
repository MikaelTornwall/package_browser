{
  "name": "reaktor-summer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../reaktor-summer-client && npm run build --prod && cp -r build ../reaktor-summer/",
    "deploy": "git push heroku master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mikael Törnwall",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
